create or replace stream actor_stream on table json_src;
create or replace stream address_stream on table json_src;
create or replace stream category_stream on table json_src;
create or replace stream city_stream on table json_src;
create or replace stream country_stream on table json_src;
create or replace stream customer_stream on table json_src;
create or replace stream film_actor_stream on table json_src;
create or replace stream film_category_stream on table json_src;
create or replace stream film_text_stream on table json_src;
create or replace stream film_stream on table json_src;
create or replace stream inventory_stream on table json_src;
create or replace stream language_stream on table json_src;
create or replace stream payment_stream on table json_src;
create or replace stream rental_stream on table json_src;
create or replace stream staff_stream on table json_src;
create or replace stream store_stream on table json_src;
